#!/usr/bin/env python

import sys
from Bicho import main

if __name__ == "__main__":
    try:
        retval = main.main()
        sys.exit (retval)
    except KeyboardInterrupt:
        print ("\n\nReceived Ctrl-C or other break signal. Exiting.")
        sys.exit (0)

